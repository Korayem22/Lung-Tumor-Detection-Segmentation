import numpy as np

def segment_full_image(image):
    """
    Placeholder for full image segmentation.
    Returns binary mask of same size as input image.
    """
    print("Mock: Running full image segmentation...")
    return np.zeros(image.shape[:2], dtype=np.uint8)
